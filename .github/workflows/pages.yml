name: Deploy production to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - name: Validate staging safeguards
        run: npm test
      - name: Build production artifact explicitly
        run: npm run build:production
      - name: Validate production artifact
        run: npm run launch:validate:production && npm run validate:production && npm run basepath:validate:production && npm run theme-urls:validate:production
      - uses: actions/upload-pages-artifact@v3
        with: { path: _site }
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
